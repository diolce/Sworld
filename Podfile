# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

plugin 'cocoapods-keys', {
    :project => "SWorld",
    :keys => [
        "TheMovieDBApiKey"
    ]
}

target 'SWorld' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SWorld
  pod 'RealmSwift'
  pod 'RxSwift'
  pod 'RxCocoa'

  target 'SWorldTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SWorldUITests' do
    # Pods for testing
  end

end
